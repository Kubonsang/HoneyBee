{
  "name": "honeybee-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main/main/main.js",
  "scripts": {
    "build": "vite build && vite build --config vite.preload.config.ts && tsc -p tsconfig.main.build.json --pretty false",
    "start": "electron .",
    "typecheck": "tsc -p tsconfig.main.json --noEmit --pretty false && tsc -p tsconfig.preload.json --noEmit --pretty false && tsc -p tsconfig.renderer.json --noEmit --pretty false"
  },
  "dependencies": {
    "@honeybee/control-plane-contracts": "workspace:*",
    "honeybee-cli": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "electron": "^43.4.0",
    "vite": "^8.2.1"
  },
  "license": "MIT"
}
